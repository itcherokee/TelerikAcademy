var app = app || {};

var hr = (function (scope) {
    var  = (function () {
    
     function () { 
     // if(this.constructor === ){
     // throw new Error("Can't instantiate abstract class ");
     // }
     .apply(this, arguments);
     }
    
     // .inherits();
    
     .prototype.toString = function () {
     // Only Type name
     return this.constructor.name;
     
     // Base toString() func Override. Require New elements or will be undefined after "PLUS"
     return .prototype.toString.call(this) + ;
     };
     
     
     return ;
    }());
    
    IMPORT TO THE CONSTRUCTOR!
    this.set();
    
    // Get  
    .prototype.get = function(){
     return this._;
    }
    
    // Set  
    .prototype.set = function(){
    // Check The Document for required validation on that property. May you lose some point if you don't have validation!!!!
     this._ = ;
    }

}(app));