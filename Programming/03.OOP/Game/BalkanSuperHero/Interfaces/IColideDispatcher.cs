namespace BalkanSuperHero.Interfaces
{
    // Responsible for managing collisions as a helper class (supervisor).
    public interface IColideDispatcher
    {
        void Distance();

        void Detect();
    }
}
